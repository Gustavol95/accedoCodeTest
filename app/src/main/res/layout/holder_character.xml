<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="character"
            type="com.accedo.codetest.data.network.Character" />

        <variable
            name="listener"
            type="com.accedo.codetest.ui.home.CharacterPagedListAdapter.OnClickCharacter" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardElevation="2dp"
            app:cardCornerRadius="2dp"
            android:layout_margin="4dp"
            android:onClick="@{() -> listener.onClick(character)}">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <TextView
                    android:id="@+id/tvName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@{character.name}"
                    android:textStyle="bold"
                    android:padding="4dp"
                    android:textSize="16sp"/>
                <ImageView
                    android:id="@+id/ivThumbnail"
                    imageUrl="@{character.thumbnail.url}"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:contentDescription="@string/character_thumbnail"
                    android:scaleType="fitStart"
                    android:adjustViewBounds="true"/>

            </LinearLayout>


        </androidx.cardview.widget.CardView>
    </RelativeLayout>

</layout>